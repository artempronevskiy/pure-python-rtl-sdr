# 📡 Программно-определяемое радио (SDR) и цифровая обработка сигналов (ЦОС)
**Фундаментальный курс для инженеров-программистов**

*Добро пожаловать на стык программной инженерии, радиофизики и высшей математики.*

Этот курс предназначен для профессиональных разработчиков (в частности, на Python). Мы ликвидируем разрыв между абстрактным кодом и физической электромагнитной реальностью. Здесь мы с нуля разберем физику радиоволн, математику гетеродинирования, теорию оцифровки и алгоритмы демодуляции, доказывая каждую формулу рабочим Python-кодом.

---

## 📑 Программа курса

**Часть I: От физики вакуума к электрическому току**
1. **Электромагнитные волны и аналоговые сигналы:** Физика эфира и математика гармонических колебаний.
2. **Аналоговая обработка и спектр:** Преобразование Фурье, фильтрация и смешивание частот.
3. **Гетеродинирование (Перенос спектра):** Физика и математика нелинейного умножения сигналов.

**Часть II: Мост в цифровой мир**
4. **Аппаратная архитектура RTL-SDR:** Антенна $\rightarrow$ МШУ $\rightarrow$ Смеситель $\rightarrow$ АЦП.
5. **Теорема Котельникова (Найквиста-Шеннона) и АЦП:** Физика дискретизации.
6. **Квадратурная дискретизация (I/Q данные):** Зачем нужны комплексные числа и формула Эйлера.

**Часть III: Цифровая обработка сигналов в коде (ЦОС)**
7. **Цифровая фильтрация и децимация:** Борьба с алиасингом (FIR vs IIR фильтры).
8. **Амплитудная модуляция (AM):** Физика огибающей и извлечение корня.
9. **Однополосная модуляция (SSB):** Преобразование Гильберта и спектральное сворачивание.
10. **Частотная модуляция (FM):** Высшая математика мгновенной фазы и комплексное сопряжение.

---

# Часть I: От физики вакуума к электрическому току

## 1. Электромагнитные волны и аналоговые сигналы

### Физика процесса
Радиоволна — это возмущение электромагнитного поля, распространяющееся в пространстве со скоростью света ($c \approx 3 \cdot 10^8$ м/с). Когда эта волна пересекает металлический проводник (антенну), переменное магнитное поле порождает индукционный переменный ток (электродвижущую силу). 

Таким образом, невидимое поле превращается в **аналоговый сигнал** — непрерывно изменяющееся во времени напряжение $u(t)$.

### Математическая модель
Идеальная радиоволна описывается уравнением гармонического колебания (несущей):
$$ u(t) = A(t) \cdot \cos(2\pi f_c t + \phi(t)) $$
Где:
*   $A(t)$ — мгновенная амплитуда (напряжение в вольтах).
*   $f_c$ — несущая частота (Герцы, колебания в секунду).
*   $\phi(t)$ — фаза (сдвиг волны во времени).

Если мы хотим передать информацию (голос, видео), мы должны изменять (модулировать) один из этих трёх параметров во времени. Голос человека занимает полосу частот от 20 Гц до 20 кГц. Антенна для передачи 20 Гц должна иметь длину в тысячи километров. Поэтому мы "сажаем" низкочастотный звук на высокочастотную радиоволну (например, 100 МГц), для которой нужна антенна длиной 75 см.

---

## 2. Аналоговая обработка и спектр

Сигнал $u(t)$ живёт во **временной области**. Но для инженеров это неудобно: глядя на осциллограмму суммы десяти радиостанций, невозможно вычленить одну. 

На помощь приходит **Преобразование Фурье**, которое переводит сигнал из временной области в **частотную (спектр)**. Фурье доказал, что любая сложная волна — это бесконечная сумма простых синусоид разных частот.

### Аналоговые фильтры
Аналоговая обработка строится на пассивных радиодеталях:
*   **Конденсатор (C):** Пропускает высокие частоты, блокирует низкие (постоянный ток).
*   **Катушка индуктивности (L):** Пропускает низкие частоты, блокирует высокие.
Комбинируя их в LC-контуры, радиоинженеры создают аналоговые фильтры, которые "вырезают" нужные куски спектра прямо в железе.

---

## 3. Гетеродинирование (Перенос спектра)

**Гетеродин** (Local Oscillator, LO) — это сердце любого радиоприемника. 
Допустим, мы хотим принять станцию на частоте $f_{RF} = 100$ МГц. Проектировать усилители и фильтры, которые можно плавно перенастраивать от 1 до 2000 МГц, невероятно сложно и дорого.

Вместо этого мы берем весь входящий спектр и **сдвигаем его вниз** до фиксированной промежуточной частоты (Intermediate Frequency, IF), например, до 0 Гц (Baseband). Таким образом, вся последующая электроника работает на одной фиксированной низкой частоте.

### Физика и математика смесителя
Как сдвинуть частоту? Нужно физически **перемножить** сигнал из антенны с сигналом локального генератора (гетеродина) внутри чипа.
В аналоговой электронике это делает компонент, называемый *смесителем* (Mixer).

Пусть $u_{RF}(t) = \cos(2\pi f_{RF} t)$ — сигнал станции (100 МГц).
Пусть $u_{LO}(t) = \cos(2\pi f_{LO} t)$ — сигнал нашего генератора (100 МГц).

Перемножаем:
$$ u_{mix}(t) = \cos(2\pi f_{RF} t) \cdot \cos(2\pi f_{LO} t) $$

Применяем тригонометрическое тождество произведения косинусов:
$$ u_{mix}(t) = \frac{1}{2} \cos(2\pi [f_{RF} - f_{LO}] t) + \frac{1}{2} \cos(2\pi [f_{RF} + f_{LO}] t) $$

**Смотрите, что произошло!** Из умножения двух частот мы получили сумму двух новых:
1.  **Разностная частота:** $100 - 100 = 0$ Гц (Baseband). Наш сигнал идеальным образом упал вниз!
2.  **Суммарная частота:** $100 + 100 = 200$ МГц. Этот мусор мы просто срезаем дешевым аналоговым фильтром нижних частот (LPF).

```python
# Демонстрация: программный аналог гетеродина (перенос частоты)
import math

def heterodyne(rf_signal: list[float], f_rf: float, f_lo: float, sample_rate: float) -> list[float]:
    mixed = []
    for i, sample in enumerate(rf_signal):
        time = i / sample_rate
        # Локальный генератор
        lo_wave = math.cos(2.0 * math.pi * f_lo * time)
        # Умножение (смешивание)
        mixed.append(sample * lo_wave)
    # После этого необходимо применить LPF фильтр
    return mixed
```

---

# Часть II: Мост в цифровой мир

## 4. Аппаратная архитектура RTL-SDR

Свисток RTL-SDR за 30$ состоит из двух чипов:
1. **Тюнер (R820T2):** Работает чисто в аналоге. 
   - **LNA (Малошумящий усилитель):** Усиливает нановольты из антенны.
   - **Гетеродин + Смеситель:** Переносит спектр выбранной вами частоты вниз до промежуточной частоты (IF = 3.57 МГц).
   - **LPF (ФНЧ):** Обрезает суммарную частоту и лишний шум.
2. **АЦП и DDC (RTL2832U):** Оцифровывает аналоговый сигнал (IF) с частотой 28.8 МГц. Затем встроенный цифровой сопроцессор окончательно сдвигает сигнал в 0 Гц, понижает частоту выборки и отправляет сырые байты по USB в компьютер.

---

## 5. Дискретизация и Теорема Котельникова (Найквиста)

Аналоговый сигнал непрерывен. Чтобы обработать его программно, Аналогово-Цифровой Преобразователь (АЦП) делает мгновенные "снимки" напряжения через равные промежутки времени. 

**Теорема Котельникова гласит:** 
> Чтобы без потерь восстановить непрерывный сигнал, частота дискретизации (Sampling Rate, $F_s$) должна быть строго более чем в 2 раза выше максимальной частоты в спектре сигнала ($F_s > 2 \cdot F_{max}$).

Если мы хотим "оцифровать" кусок радиоэфира шириной 2 МГц, АЦП обязан работать на частоте минимум 4 МГц. Если частота будет ниже, возникнет **Алиасинг (Aliasing)** — высокие частоты зеркально отразятся и наложатся на низкие, уничтожив сигнал (какофония).

---

## 6. Квадратурная дискретизация (I/Q данные)

Инженеры SDR используют математический трюк, чтобы нарушить правило $2x$ и сканировать полосу 2 МГц при частоте АЦП ровно 2 МГц. Этот трюк — использование **Комплексных чисел (I/Q данных)**.

### Проблема одномерного (вещественного) сигнала
Если вы оцифровываете простое напряжение $u(t) = \cos(\omega t)$, вы теряете фазу. Остановив время, вы не знаете, куда пойдет волна дальше — вверх или вниз. 

Более того, математически вещественный косинус является суммой вращений в *две противоположные стороны*:
$$ \cos(\omega t) = \frac{e^{j\omega t} + e^{-j\omega t}}{2} $$
Это значит, что станция, находящаяся на смещении $+100$ кГц от нуля, и станция на смещении $-100$ кГц будут выглядеть в цифровом виде **абсолютно одинаково** и разрушат друг друга.

### Решение: I/Q (In-phase / Quadrature) выборка
Мы берем сигнал с антенны, разветвляем его надвое и подаем на два параллельных смесителя. Но на второй смеситель мы подаем гетеродин, сдвинутый на **90 градусов** (косинус и синус):
*   **Канал I (In-phase):** Умножаем на $\cos(\omega_{LO} t)$.
*   **Канал Q (Quadrature):** Умножаем на $-\sin(\omega_{LO} t)$.

Теперь каждый "сэмпл" сигнала — это пара напряжений $(I, Q)$, которая математически формирует **комплексное число**: $z = I + jQ$.

По **формуле Эйлера** ($e^{j\theta} = \cos\theta + j\sin\theta$), такой комплексный сигнал (Аналитический сигнал) описывает конец вектора, вращающегося на декартовой плоскости!
*   Вектор вращается против часовой $\rightarrow$ частота положительная.
*   Вектор вращается по часовой $\rightarrow$ частота отрицательная.

Мы можем идеально различать станции справа и слева от центральной частоты!

```python
# Чтение сырых 8-битных I/Q данных от RTL-SDR в Python
# Байты идут попарно: [I0, Q0, I1, Q1, ...]
# Значение 127.5 означает 0 Вольт.

n_pairs = len(raw_bytes) // 2
complex_signal = []

for k in range(n_pairs):
    i_raw = raw_bytes[k*2]     - 127.5  # Приводим к диапазону [-127.5, +127.5]
    q_raw = raw_bytes[k*2 + 1] - 127.5
    
    # Теперь это математическая точка на плоскости
    z = complex(i_raw, q_raw)
    complex_signal.append(z)
```

---

# Часть III: Цифровая обработка сигналов в коде (ЦОС)

## 7. Цифровая фильтрация и децимация

АЦП RTL-SDR выдает данные с гигантской скоростью (н-р, 2 400 000 сэмплов в секунду). Аудиокарта ждет 48 000 сэмплов в секунду. Нам нужно понизить частоту дискретизации (Децимация) в $D = 50$ раз.

### Борьба с алиасингом
Если просто взять каждый 50-й сэмпл (`array[::50]`), то высокочастотный радиошум, согласно теореме Котельникова, сложится гармошкой и уничтожит наш полезный низкочастотный звук. 

**Правило:** Перед каждой децимацией в $D$ раз, сигнал обязан пройти Фильтр Нижних Частот (ФНЧ / LPF), который жестко срежет все частоты выше $F_{new}/2$.

### Алгоритм Box-car (Скользящее среднее)
В аналоге мы ставили катушки и конденсаторы (IIR фильтры). В цифре мы используем **КИХ-фильтры** (FIR, Finite Impulse Response), которые работают как математическая свертка.

Для идеального баланса скорости (в Python) мы применяем фильтр `Box-car` — сумму $N$ соседних элементов. 
Согласно распределению Фурье, прямоугольное окно во временной области превращается в функцию `sinc(x)` ($\sim \sin(x)/x$) в частотной области. Эта функция имеет глубокие нули (идеальное подавление) ровно на тех частотах, которые вызовут алиасинг!

```python
# Интегрированная децимация и КИХ-фильтрация (антиалиасинг) 
# Понижение частоты в 5 раз
n_decimated = len(i_full) - (len(i_full) % 5)
inv5 = 1.0 / 5.0

audio_baseband = [
    # Усреднение 5 сэмплов выполняет роль LPF-свертки (sinc фильтр)
    (i_full[j] + i_full[j+1] + i_full[j+2] + i_full[j+3] + i_full[j+4]) * inv5
    for j in range(0, n_decimated, 5)  # Шаг 5 осуществляет децимацию
]
```

---

## 8. Амплитудная модуляция (AM)

**Физика:** 
В AM низкочастотный звук $m(t)$ просто меняет высоту (напряжение) радиоволны.
$$ u_{AM}(t) = [A_0 + m(t)] \cos(2\pi f_c t) $$
Спектр AM состоит из мощной ненужной Несущей ($A_0$) и двух одинаковых боковых полос, содержащих звук.

**Алгоритм (Детектор огибающей):**
Когда RTL-SDR переносит этот сигнал в 0 Гц (Baseband), несущая становится постоянным током (DC), а звук расходится вправо и влево.
Вся информация о звуке зашифрована исключительно в **Длине** вращающегося комплексного I/Q вектора. Его угол (фаза) нас не интересует.

Чтобы извлечь звук $m(t)$, нужно применить теорему Пифагора — найти модуль (гипотенузу) каждого комплексного сэмпла.

```python
# Демодулятор AM
audio_env = []
for k in range(n_pairs):
    i = raw[k*2] - 127.5
    q = raw[k*2+1] - 127.5
    
    # Геометрическая длина вектора — это и есть звук!
    amplitude = math.sqrt(i**2 + q**2)
    audio_env.append(amplitude)

# Так как амплитуда всегда > 0, сигнал имеет гигантское смещение (DC).
# Фильтр высоких частот (ФВЧ / HPF) удаляет остаток несущей частоты:
dc = 0.0
alpha = 0.001
for idx in range(len(audio_env)):
    dc += alpha * (audio_env[idx] - dc)
    audio_env[idx] -= dc
```

---

## 9. Однополосная модуляция (SSB: LSB / USB)

**Физика:** 
Обычная АМ тратит 66% энергии передатчика вхолостую на несущую, и еще дублирует спектр. SSB решает эту проблему. Передатчик вырезает несущую полностью (Suppressed Carrier) и удаляет одну из боковых полос. 
В эфир уходит только узкая полоса в 3 кГц. Вся мощность идет только в полезный сигнал.
*   **USB (Upper Sideband):** Положительные частоты от несущей.
*   **LSB (Lower Sideband):** Отрицательные частоты от несущей (спектр перевернут).

**Алгоритм (Спектральное сворачивание):**
Если наш SDR настроен точно туда, где раньше была удаленная несущая, то сигнал спускается в идеальный **комплексный Baseband**.
*   USB-сигнал живет *только в положительной* полуплоскости частот.
*   LSB-сигнал живет *только в отрицательной* полуплоскости частот.

Комплексный вектор умеет различать + и -. Но одномерный **вещественный звук**, который мы слышим ушами, математически всегда симметричен (содержит обе полуплоскости наложенными друг на друга).

Секрет SSB демодуляции невероятно элегантен: чтобы превратить однобокий положительный/отрицательный спектр в симметричный слышимый звук, нужно просто **выбросить мнимую ось (Q)**. Взятие реальной части $\Re(z)$ математически заставляет невидимый отрицательный спектр сложиться с положительным.

```python
# Демодулятор SSB (наивысшая простота благодаря комплексной природе)
# Никаких детекторов, корней и углов. Real(I) канал — это уже готовый звук!

for k in range(n_pairs):
    # Просто забираем I канал. Ось Q игнорируется.
    audio[k] = raw[k*2] - 127.5

# Дальше идет децимация (сглаживание шума) и DC-фильтр.
```
*(Генерация SSB на стороне передачи гораздо сложнее: она требует Преобразования Гильберта — фазового сдвига всего звука на 90 градусов для создания аналитического сигнала, чтобы деструктивно подавить одну из полос в эфире).*

---

## 10. Частотная модуляция (FM)

**Физика:**
АМ радио сильно подвержено помехам (молнии — это физические всплески амплитуды). 
В FM мы держим амплитуду строго неизменной. Звук $m(t)$ кодируется в **скорости вращения вектора** (в мгновенной частоте).
$$ u_{FM}(t) = A_0 \cos\left(2\pi f_c t + 2\pi k_f \int m(\tau) d\tau\right) $$

**Алгоритм (Полярный дискриминатор):**
Согласно законам дифференциального исчисления, Частота — это **первая производная фазы по времени**: $f(t) = \frac{1}{2\pi} \frac{d\theta}{dt}$.

Значит, алгоритм таков:
1. Вычисляем текущий угол комплексного числа: $\theta_n = \arctan(Q/I)$
2. Находим разность (производную): $\Delta\theta = \theta_n - \theta_{n-1}$. Это и есть наш звук!

Однако, вычислять тяжелый `atan2` дважды за сэмпл — смерть для CPU. К тому же простое вычитание углов порождает ошибки перехода (e.g. $359^\circ - 1^\circ$ даст $+358^\circ$, хотя кратчайший путь это $-2^\circ$).

Трюк: Умножение текущего комплексного числа на **комплексно-сопряженное** от предыдущего ($Z_n \cdot \overline{Z_{n-1}}$) математически выполняет безопасное вычитание их углов в геометрическом пространстве!

```python
# Демодулятор FM (Вычисление мгновенной производной фазы)
for k in range(n_pairs):
    i_curr = raw[k*2] - 127.5
    q_curr = raw[k*2+1] - 127.5

    # Комплексное перемножение: Z_curr * conj(Z_prev)
    # Алгебраическое раскрытие устраняет вызов двух atan2
    p_i = i_curr * p_prev_i + q_curr * p_prev_q
    p_q = q_curr * p_prev_i - i_curr * p_prev_q

    # Сохраняем сэмпл для следующего шага
    p_prev_i, p_prev_q = i_curr, q_curr

    # Финальный atan2 возвращает готовую идеальную разницу фаз (звук) в рамках [-π, +π]
    audio_fm[k] = math.atan2(p_q, p_i)
```

---

## Итог курса
ЦОС позволяет отказаться от конденсаторов и паяльников в пользу чистой детерминированной математики. Овладев геометрией комплексного вектора, законами алиасинга Найквиста и дифференциальным исчислением огибающих, вы теперь можете написать код для перехвата данных с самолетов, расшифровки снимков с орбитальных спутников погоды или создания собственной зашифрованной радиостанции прямо в консоли Python.

*Удачного программирования.* 📡🐍
